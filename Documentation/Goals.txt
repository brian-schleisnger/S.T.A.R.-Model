The things I'm looking to add/update, roughly in the order in which I'm trying to achieve them, knowing I likely won't achieve them all. 
1. Boost model reliability - make sure when the user asks questions, the output is what the user wants
2. Connect the project to actual post-acquisition customer data, either by the redshift tables with some kind of service account or through talking to Kyle/Ryan/Zach's team about connecting to their data.
4. Make the visualizations better
5. what-if scenario better
6. add in unstructured data telling us what retail prices have been over the past years 
7. Make sure all of the ML models are visible to the end user: a.k.a. report the parameter values, the tree, the weights, something
8. actually just update the download feature to also report what tool the downloads were from and the arguments as well, so especially I can re-run sql queries is nesc. 
